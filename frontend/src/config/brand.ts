// Central brand config — the single source of truth for app identity.
// Keep every surface (customer, admin, warehouse, rider, auth, PWA) consistent
// by importing from here instead of hardcoding the name.

export const APP_NAME = 'Zippy';
export const APP_TAGLINE = 'Groceries in minutes';
export const DELIVERY_PROMISE = 'Delivery in 10 minutes';

// Blinkit-style signature yellow (identity surfaces) + dark ink text on it.
export const BRAND_YELLOW = '#f8cb46';
export const BRAND_INK = '#1c1c28';
